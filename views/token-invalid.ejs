<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Link Expired</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      :root {
        --nav-bg: linear-gradient(135deg, #663399, #9370db);
        --nav-accent: #dd88cf;
        --nav-text: #f8e7f6;
        --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      body {
        background: var(--nav-bg);
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--nav-text);
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      .card {
        background-color: rgba(255, 255, 255, 0.1);
        border: none;
        border-radius: 20px;
        padding: 2rem;
        text-align: center;
        color: var(--nav-text);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        transition: var(--transition);
      }

      .card:hover {
        transform: scale(1.02);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
      }

      .btn-home {
        background-color: var(--nav-accent);
        color: #fff;
        border: none;
        transition: var(--transition);
      }

      .btn-home:hover {
        background-color: #c76abb;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h1 class="mb-3">🚫 Link Expired or Already Used</h1>
      <p>The invitation link you tried to access is no longer valid.</p>
      <a href="/" class="btn btn-home mt-3 px-4">Go to Home</a>
    </div>
  </body>
</html>
